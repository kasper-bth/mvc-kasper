{% extends "base.html.twig" %}

{% block title %}API{% endblock %}

{% block body %}
    <div class="container">
        <h1>JSON API</h1>
        <p>De json routes som finns är:</p>
        <ul>
            <li><a href="{{ path('api_lucky') }}">api/lucky</a>, Denna route visar ett nummer mellan 1-100.</li>
            <li><a href="{{ path('api_quote') }}">api/quote</a>, Denna route visar en av tre citat.</li>
            <li><a href="{{ path('api_deck') }}">api/deck</a>, Denna route visar den sorterade kortleken.</li>
            <li><a href="{{ path('api_deck_shuffle') }}">api/deck/shuffle</a>, Denna route blandar kortleken.</li>
            <li><a href="{{ path('api_deck_draw') }}">api/deck/draw</a>, Denna route drar ett kort från kortleken.</li>
            <li><a href="{{ path('api_deck_draw_number', {number: 5}) }}">api/deck/draw/number</a>, Denna route drar 
            ett nummer av kort från kortleken.</li>
            <li><a href="{{ path('api_game', {number: 5}) }}">api/game</a>, Denna route visar den aktuella 
            ställningen för kortspelet 21.</li>
            <li><a href="{{ path('api_library_books') }}">api/library/books</a>, Denna route visar alla böcker i databasen.</li>
            <li><a href="{{ path('api_library_book', {isbn: 9780393972832}) }}">api/library/book/{isbn}</a>, Denna route visar
            en specifik bok i databasen.</li>
        </ul>
    </div>
    <footer class="footer">
        <p>&copy; Copyright ägs av mig och bilder tillåts att användas och har hämtats från wikipedia.</p>
    </footer>
{% endblock %}